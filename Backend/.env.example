PORT= "port number"
MONGODB_URI= "keep Mongodb_url"
JWT_SECRET= "Keep your jwt_secret "
JWT_EXPIRES_IN=7d
SMTP_HOST="enter smtp host provider"
SMTP_PORT="port of smtp "
SMTP_USER="mockuser"
SMTP_PASS="mockpass"
SMTP_FROM="pass the smtp from the email"
